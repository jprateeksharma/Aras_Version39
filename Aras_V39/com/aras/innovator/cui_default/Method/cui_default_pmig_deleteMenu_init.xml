<AML>
 <Item type="Method" id="FA9A14FE1FE14E5AAD923A556A2B8066" action="add">
  <execution_allowed_to keyed_name="World" type="Identity">A73B655731924CD0B027E4F4D5FCC0A9</execution_allowed_to>
  <method_code><![CDATA[const {
	getState,
	itemType,
	rows,
	polySources,
	commandFactory
} = options;
const itemTypeMetadata = ArasModules.metadata.itemType;
const isPolymorphic = itemTypeMetadata.isPolymorphic(itemType.id);
const getItemType = (item) => {
	if (isPolymorphic) {
		const itemTypeId = item.itemtype;
		const itemTypeName = itemTypeMetadata.getItemTypeName(itemTypeId);
		return polySources[itemTypeName];
	}

	return itemType;
};

const { grid } = getState();
const selectedRows = grid.selectedRows.map(rowId => {
	const item = rows.get(rowId);
	const itemType = getItemType(item);
	const deleteCommand = commandFactory('DeleteItem', {
		...options,
		item,
		itemType,
		itemTypeName: itemType.name
	});
	const isDeleteCommandAllowed = deleteCommand.canExecute();

	return {
		isDeleteCommandAllowed,
		isVersionable: itemType.is_versionable === '1'
	};
});

const hidden = selectedRows.every(row => !row.isVersionable);
if (hidden) {
	return { hidden };
}

const disabled = selectedRows.every(row => !row.isDeleteCommandAllowed);

return {
	disabled,
	hidden: false
};]]></method_code>
  <method_type>JavaScript</method_type>
  <name>cui_default_pmig_deleteMenu_init</name>
 </Item>
</AML>
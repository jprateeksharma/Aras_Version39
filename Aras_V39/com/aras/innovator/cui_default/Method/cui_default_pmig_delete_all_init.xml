<AML>
 <Item type="Method" id="1985416DBAAA4E03A8CAFAE5D1FB4202" action="add">
  <execution_allowed_to keyed_name="World" type="Identity">A73B655731924CD0B027E4F4D5FCC0A9</execution_allowed_to>
  <method_code><![CDATA[const {
	getState,
	itemType,
	polySources,
	commandFactory
} = options;
const itemTypeMetadata = ArasModules.metadata.itemType;
const isPolymorphic = itemTypeMetadata.isPolymorphic(itemType.id);
const getItemType = (item) => {
	if (isPolymorphic) {
		const itemTypeId = item.itemtype;
		const itemTypeName = itemTypeMetadata.getItemTypeName(itemTypeId);
		return polySources[itemTypeName];
	}

	return itemType;
};
const commandAlias = target['command_alias@keyed_name'];
const { grid } = getState();
const disabled = grid.selectedRows.every(rowId => {
	const item = options.rows.get(rowId);
	const itemType = getItemType(item);
	const deleteCommand = commandFactory(commandAlias, {
		... options,
		item,
		itemType,
		itemTypeName: itemType.name
	});
	return !deleteCommand.canExecute() || window.isFunctionDisabled(itemType.name, 'Delete');
});

return { disabled };]]></method_code>
  <method_type>JavaScript</method_type>
  <name>cui_default_pmig_delete_all_init</name>
 </Item>
</AML>